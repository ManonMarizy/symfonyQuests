{% extends 'base.html.twig' %}

{% block main %}
    <h1 class="text-center mt-4">Actor</h1>
    <div class="m-4">
        <h2>{{ actor.name }}</h2>
        <h4>Séries:</h4>
        <ul>
            {% for program in actor.programs%}
                <li class="list-unstyled"><a href="{{ path("program_show", { id: program.id }) }}">{{ program.title }}</a></li>
            {% endfor %}
        </ul>
    </div>
{%  endblock %}