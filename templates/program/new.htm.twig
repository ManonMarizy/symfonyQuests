{% extends "base.html.twig" %}

{% block main %}
    <h1 class="text-center mt-3">Ajouter une série</h1>
    <div class="d-flex flex-column align-items-center mt-5">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type='submit' value='Ajouter' class="mt-3">
        {{ form_end(form) }}
    </div>
{% endblock %}