{% extends 'base.html.twig' %}

{% block title %}Série #{{ program.id }}{% endblock %}

{% block main %}
    <div class="media mt-4 mr-3">
        <img class="align-self-start mr-2" src="{{program.poster}}" alt="{{ program.title }} poster">
        <div class="media-body ml-2">
            <h1 class="mt-0">{{ program.title }}</h1>
            <p>{{ program.summary }}</p>
            <p>Catégorie : {{ program.category.name }}</p>
            <ul class="list-unstyled">
                {% for season in seasons %}
                    <li class="ml-3"><a href="{{ path("program_season_show", { programId: program.id, seasonId: season.id }) }}">Saison {{ season.id }}</a></li>
                {% endfor %}
            </ul>
            <a href="{{ path('program_index') }}">
                Retour à la liste des programmes
            </a>
        </div>
    </div>
{% endblock %}